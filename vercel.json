{
  "buildCommand": "ng build --configuration production",
  "outputDirectory": "dist/InterCommerceWebApp",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}